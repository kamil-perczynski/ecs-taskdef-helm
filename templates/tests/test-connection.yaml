apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ecs-example.fullname" . }}-test-connection"
  labels:
    {{- include "ecs-example.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ecs-example.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
