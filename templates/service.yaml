apiVersion: v1
kind: Service
metadata:
  name: {{ include "ecs-example.fullname" . }}
  labels:
    {{- include "ecs-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ecs-example.selectorLabels" . | nindent 4 }}
